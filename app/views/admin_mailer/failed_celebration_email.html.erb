<!DOCTYPE html>
<Body>
    <h1>Celebration Failed</h1>
    Today's
    <% if @celebration_event.birthday? %>
    birthday
    <% elsif @celebration_event.work_anniversary? %>
    work anniversary
    <% end %>
    celebration of <%= @celebration_event.recipient.first_name %> <%= @celebration_event.recipient.last_name %>
    failed.
</body>
